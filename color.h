#ifndef COLOR_H
#define COLOR_H

#include <cstdint>
#include <cassert>

struct Color {
  uint8_t r, g, b;

  // member functions for doing operations on Color values
  // could be helpful
};

#endif // COLOR_H
